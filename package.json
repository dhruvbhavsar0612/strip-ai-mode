{
  "name": "strip-ai-mode",
  "version": "1.0.0",
  "private": true,
  "description": "Dev tooling for the No AI Overviews for Google Search extension. The extension itself has no runtime dependencies.",
  "scripts": {
    "lint": "eslint .",
    "validate": "web-ext lint --source-dir . --ignore-files dist/** node_modules/**"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.4.0",
    "globals": "^17.6.0",
    "web-ext": "^10.3.0"
  }
}
